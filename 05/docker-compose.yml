version: "3.9"

services:
  immich-server:
    container_name: immich_server
    image: ghcr.io/immich-app/immich-server:release
    environment:
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_USERNAME=${DB_USERNAME}
      - DB_DATABASE_NAME=${DB_DATABASE_NAME}
      - DB_HOSTNAME=immich_postgres
      - REDIS_HOSTNAME=immich_redis
      - IMMICH_VERSION=release
    volumes:
      - ${UPLOAD_LOCATION}:/usr/src/app/upload
      - /etc/localtime:/etc/localtime:ro
    ports:
      - 2283:2283
    depends_on:
      - redis
      - database
    networks:
      - web_network
    restart: always

  immich-machine-learning:
    container_name: immich_machine_learning
    # Versão otimizada para ARM64
    image: ghcr.io/immich-app/immich-machine-learning:release
    volumes:
      - model-cache:/cache
    networks:
      - web_network
    restart: always

  redis:
    container_name: immich_redis
    image: docker.io/valkey/valkey:9
    networks:
      - web_network
    restart: always

  database:
    container_name: immich_postgres
    image: ghcr.io/immich-app/postgres:16-vector
    environment:
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_USER=${DB_USERNAME}
      - POSTGRES_DB=${DB_DATABASE_NAME}
      - POSTGRES_INITDB_ARGS=--data-checksums
    volumes:
      - ${DB_DATA_LOCATION}:/var/lib/postgresql/data
    shm_size: 256mb # Aumentado para o processamento de vetores de imagem
    networks:
      - web_network
    restart: always

volumes:
  model-cache:

networks:
  web_network:
    external: true
# Variaveis usadas
# DB_PASSWORD: (escolhe uma forte)
# DB_USERNAME:
# DB_DATABASE_NAME:
# UPLOAD_LOCATION:  (pasta onde queres guardar as fotos).
# DB_DATA_LOCATION: (pasta onde queres processar as fotos).
