services:
  rainloop:
    image: lscr.io/linuxserver/rainloop:latest
    container_name: rainloop
    restart: always
    networks:
      - web_network
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Lisbon
    volumes:
      - ${DATA}:/config
    expose:
      - "80"
